{
  "name": "exam-platform",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "postinstall": "cd backend && npm install --omit=dev",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": "22.x"
  }
}
